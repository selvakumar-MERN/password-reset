import React from 'react';

function Invalid(props) {
    return (
        <div className='container'>
            <h2>Sorry Invalid Link or invalid token</h2>
            
            
        </div>
    );
}

export default Invalid;